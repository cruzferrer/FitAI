// Re-export from lib/utils for cleaner organization
// app/utils should not contain business logic, but this allows backward compatibility
export { normalizeAndExpandRutina } from "../../lib/utils/expandRoutine";

export default {};
